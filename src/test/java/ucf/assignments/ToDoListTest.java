package ucf.assignments;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

class ToDoListTest {
    @Test
    void addList() {
        // Adds the new to do list to the array of all the other lists
        // Test if the new list was actually added to the array
    }
    @Test
    void addItem() {
        // Adds the new item of the to do list to the array belonging to the correct to do list
        // Test true if the new item was successfully added to the correct to do list
    }
}