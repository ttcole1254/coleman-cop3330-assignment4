package ucf.assignments;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

class ToDoListItemTest {
    @Test
    void changeItemDueDate() {
        // When an item's due date is modified, we can test if the date is still the old one and if it's in the correct format.
    }
    @Test
    void getItemDesc() {
        // When an item's description is fetched, ensure it matches a known value.
    }
    @Test
    void changeItemDesc() {
        // When an item's description is modified, we can test to ensure it is the new value.
    }
}